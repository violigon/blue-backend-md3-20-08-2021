const nome = 'Calculadora do Vini';

const soma = (a,b) => a + b; 

const sub = (a,b) => a - b;

const mult = (a,b) => a * b;

const div = (a,b) => a / b;

module.exports = {
    nome,
    soma,
    sub,
    mult,
    div
};